name: preflight-extractor
version: "1.0.0"
description: Extract partial structured brief from free text.

model:
  preferred: openai:gpt-4o-mini

tools: [read] # sandboxed: skills/, SOUL.md, RULES.md — injected by gateway, not builtin fs

skills:
  - brief-structure

runtime:
  max_turns: 3
