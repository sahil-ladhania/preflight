name: preflight-explainer
version: "1.0.0"
description: Homepage agent — discuss campaigns and rules; hand off to Campaign.

model:
  preferred: openai:gpt-4o-mini

tools: [read] # sandboxed: skills/, SOUL.md, RULES.md — injected by gateway, not builtin fs

skills:
  - homepage-journeys
  - rule-explain

runtime:
  max_turns: 3
