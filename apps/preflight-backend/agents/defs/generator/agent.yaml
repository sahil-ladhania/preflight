name: preflight-generator
version: "1.0.0"
description: Write channel marketing copy under frozen compliance rules and brand kit.

model:
  preferred: openai:gpt-4o-mini

tools: [read] # sandboxed: skills/, SOUL.md, RULES.md — injected by gateway, not builtin fs

skills:
  - brand-voice
  - channel-email
  - channel-linkedin
  - channel-display
  - channel-shortform
  - sebi-copy-constraints
  - channel-tiktok

runtime:
  max_turns: 3
