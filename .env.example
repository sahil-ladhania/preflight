DATABASE_URL=postgresql://user:password@localhost:5432/preflight
OPENAI_API_KEY=sk-your-key-here
# Optional — defaults to model.preferred in each agent.yaml (openai:gpt-4o-mini)
# OPENAI_MODEL=openai:gpt-4o-mini
DEMO_OPERATOR_NAME=Demo Operator
# Named limitation: dump SKILL.md bodies into system prompt. Happy path is catalog + read.
# PREFLIGHT_SKILL_DUMP=1
